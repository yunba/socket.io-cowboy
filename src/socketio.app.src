{application, socketio, [
	{description, "Socketio for cowboy."},
	{vsn, "0.1.0"},
	{modules, []},
	{registered, [socketio_sup]},
	{applications, [
		kernel,
		stdlib,
		crypto,
		asn1,
		public_key,
		ssl,
		ranch,
		cowlib,
		cowboy,
		mnesia,
		resource_discovery
	]},
	{mod, {socketio, []}},
	{env, []}
]}.
